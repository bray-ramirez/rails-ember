{{#if isEditable}}
  {{post-edit model=model onUpdate=(action "updateRecord") onCancel=(action "postViewable")}}
{{else}}
  {{post-static model=model onEditable=(action "postEditable") onDelete=(action "deleteRecord")}}
{{/if}}
